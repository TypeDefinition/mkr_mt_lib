#pragma once

namespace mkr {
    /**
     * Base class for all containers in this library.
     */
    class container {
    public:
        container() = default;
        virtual ~container() = default;
    };
}